spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mymart
    username: postgres
    password: pass123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true